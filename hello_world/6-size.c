#include <stdio.h>

/**
 * main-prints the size of various types
 *
 * Return: always 0
 */
 * int main(void)
 *{
	 printf("size of a char:", size of(char));
	 printf("size of an int:", size of(int));
	 printf("size of a long int:", size of(long int));
	 printf("size of long long int:", size of(long long int));
	 printf("size of float:", size of(float));
	 return (0);

 }
